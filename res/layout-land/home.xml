<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/homeRelativeLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal" >

    <it.giacomos.android.wwwsapp.widgets.SituationImage
        android:id="@+id/situationImageView"
        android:layout_width="0dip"
        android:layout_height="fill_parent"
        android:layout_weight="1.0"
        android:background="@drawable/shape_background_landscape"
        android:src="@drawable/fvg_background2" />

    <ScrollView
        android:id="@+id/textViewScroll"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1.0" >

        <LinearLayout
            android:id="@+id/scrollLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" >

            <it.giacomos.android.wwwsapp.widgets.HomeTextView
                android:id="@+id/homeTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="none" >

            </it.giacomos.android.wwwsapp.widgets.HomeTextView>

            <!-- here the trial layout goes -->

        </LinearLayout>
    </ScrollView>

</LinearLayout>